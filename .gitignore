# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.parquet
berm/berm/data/cache/

# Node
node_modules/
.next/
out/
.vercel/

# Data (large files — re-downloadable)
data/raw/
berm/data/raw/*
# ...but provenance must be version-controlled, even when the bytes are not.
# Manifests carry the sha256 of every raw artefact; without them in git there is
# no record of which data vintage produced a result.
!berm/data/raw/README.md
!berm/data/raw/manifests/

# Processed data (regenerated by parse_all.py)
data/processed/
berm/data/processed/

# Global source-panel build is a 73 MB local research artefact.  Its immutable
# raw-source manifest, compact tier artefact, public CSV, and checksums are
# versioned; regenerate this full provenance panel locally when needed.
berm/data/global/all_countries_panel.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.production
.vercel
